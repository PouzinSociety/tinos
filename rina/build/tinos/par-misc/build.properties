#
# Default Entries
#
version=1.0.0
release.type=integration
natural.name=<%= project_name %>
project.key=SAMPL
bucket.basename=springsource.com

# Used for the "ant distro" task - will place deployment files in the specified directories
distro.par.dir=<%= project_dir %>/dist
distro.provided.dir=<%= project_dir %>/par-provided

# TSSG: Not using a separate integration repository at present
#       "Integration" is set to ivy-cache for the moment
#
#  integration.repo.dir=${basedir}/../integration-repo


#
# FIX-ME: Set these to match the environment.
#
# This can be placed anywhere (a shared one is good!)
ivy.cache.dir=<%= ENV['HOME'] %>/ivy/ivy-cache
# Update This to where you store your local repository
ivy.local.repo=<%= ENV['HOME'] %>/ivy/ivy-local-repo
