<?xml version="1.0" encoding="UTF-8"?>
<project name="<%= organization %>">
    <path id="bundles">
<!--
 # FIX-ME : Add the wrapped bundles here, sample provided
 #          Add a new entry for each bundle.
 -->
        <pathelement location="../<%= organization %>.bundle_a" />
    </path>

    <property file="${basedir}/../build.properties" />
	<import file="${basedir}/../build-<%= project_name %>/package-par.xml"/>
    <import file="${basedir}/../spring-build/par/default.xml" />
</project>
